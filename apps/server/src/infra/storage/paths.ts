/**
 * Re-export shim for the storage layout authority. The implementation
 * lives in `@hoardodile/host/hoard` (shared with the CLI and future
 * shells); this module only keeps the server's import sites stable.
 */
export * from "@hoardodile/host/hoard"
